export default class Aggregation {
    data: any;
    url: string;
    options: {
        [option: string]: boolean;
    };
    constructor(data: any, url: string, options: {
        [option: string]: boolean;
    });
}
