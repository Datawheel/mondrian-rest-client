export default class Aggregation {
    data: {};
    url: string;
    options: {
        [option: string]: boolean;
    };
    constructor(data: {}, url: string, options: {
        [option: string]: boolean;
    });
}
